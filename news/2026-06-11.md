# Novidades — 11 de Junho de 2026

Hoje foram realizadas melhorias estruturais, correções de empacotamento, compatibilidade e alinhamento de documentações na **Lazarus AI Suite**.

## 🏭 Organização e Correção do Pacote Industrial (`openai_industrial`)
- **Realocação Física e Lógica**: Os componentes `TAIPOSPrinter`, `TAIModbusClient`, `TAIMQTTClient` e `TAIIndustrialBridge` foram reposicionados para a aba e documentação de **AI Industrial** (removendo referências redundantes e incorretas que existiam na aba/documentação de AI Input).
- **Correção da Paleta de Componentes**: Removida a flag `<AddToUsesPkgSection Value="False"/>` do arquivo de projeto `openai_industrial.lpk` para permitir que as units de registros Modbus, IoT MQTT e POSPrinter sejam corretamente registradas na paleta da IDE do Lazarus ao instalar o pacote.
- **Documentação Unificada**: Criado o novo arquivo de documentação [README.md](file:///d:/projetos/maurinsoft/CHATGPT/pacote/DOC/AI%20Industrial/README.md) detalhando as finalidades industriais e propriedades dos componentes.

## 📷 Melhorias e Ajustes de Captura Nativa (`TAICaptureSource`)
- **Conserto da Visualização no Exemplo**: Ajustada a carga de propriedades em tempo de execução no `capture_source_demo` para associar o Handle correto do painel gráfico ao `PreviewHandle`, corrigindo o travamento que gerava o erro `"PreviewHandle is required when PreviewEnabled is True"`.
- **Compatibilidade do Backend Windows VFW**: O backend nativo de vídeo do Windows [`aicamera_vfw.pas`](file:///d:/projetos/maurinsoft/CHATGPT/pacote/AI%20Vision/aicamera_vfw.pas) foi otimizado. Adicionada a conversão explícita do caminho do arquivo temporário (`AFileName`) para `AnsiString` ao despachar a mensagem de captura de frame (`WM_CAP_FILE_SAVEDIB`), solucionando o erro de gravação de imagens DIB do VFW (`Failed to save DIB image via VFW`).
- **Migração OpenCV**: Removidas referências remanescentes ao componente obsoleto `TAICameraCapture` no demo de processamento visual `opencv_vision_demo`, unificando a integração para utilizar o novo `TAICaptureSource`.
