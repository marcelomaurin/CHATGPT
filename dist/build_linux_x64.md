# Linux x64 build

Create `dist/output` and generate `CHATGPT-AI-runtime-linux-x64.zip` from the release staging folder.

This file documents the build target when shell scripts are restricted by the connector.
