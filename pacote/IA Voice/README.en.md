# 🗣️ Documentation for IA Voice Tab

> [!NOTE]
> This folder contains the Lazarus components suite under the **IA Voice** tab.

## Voice Synthesis and Speech Signal Engine.
Native engines for text-to-speech (TTS) conversion in multiple Pascal voice timbres.

### Detailed Component Reference

| Component | Description | Important Properties | Main Methods | AI Agent Role |
|---|---|---|---|---|
| **TAIVoiceSynthesizer** | Speech and voice synthesizer. | `Pitch, Rate, Volume` | `Speak(const AText: string): Boolean` | Synthesize natural-sounding speech from summaries generated by the AI. |

### 💻 Lazarus Code Example (TAIVoiceSynthesizer)

```pascal
var
  MyComponent: TAIVoiceSynthesizer;
begin
  MyComponent := TAIVoiceSynthesizer.Create(Self);
  try
    // Configuration properties
    // MyComponent.Property := Value;
    
    // Execute call
    // MyComponent.ExecuteMethod;
  finally
    MyComponent.Free;
  end;
end;
```


### ⚡ AI and Hardware Bridge
Each of these components features a published `Prompt` property that transparently documents its internal API to guide AI Agents (`TAIAgent`) autonomously!
