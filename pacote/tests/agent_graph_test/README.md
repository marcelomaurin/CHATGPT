# Agent graph test

Valida nodes/edges, transições, checkpoint versionado, resume, aprovação,
rejeição, delegação por ID, `FreeNotification`, `Simulation` e `DryRun` sem
efeitos reais. O projeto usa verificações de runtime e `heaptrc`.
