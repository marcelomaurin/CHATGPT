# observability_test

Valida TraceID, timestamps, eventos, spans e propagacao por Agent, Tool, RAG e
LLM. Tambem confirma metricas de uso reais quando o mock HTTP esta ativo e
garante que prompts/argumentos nao aparecem com a configuracao segura padrao.
