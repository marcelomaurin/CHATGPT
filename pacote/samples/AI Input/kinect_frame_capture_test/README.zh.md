# kinect_frame_capture_test

控制台测试程序，使用包中的 Kinect 组件打开传感器，采集第一个彩色帧，并保存到磁盘。

## 输出

- 日志：`capture_output\kinect_frame_capture_test.log`
- 图像：`capture_output\captured_color_frame.bmp`

## 何时使用

当图形演示没有图像，或需要在无界面情况下验证采集时使用。

## 要求

Windows、Kinect SDK/Runtime 1.8、`Kinect10.dll`，以及已连接的 Kinect。